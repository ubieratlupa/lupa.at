<div class="page-menu">
	<div class="page-menu-inner">
		<% if current_page?(:controller => 'pages', :action => 'index') %>
			<%= link_to "Start", {controller: "pages", action: "index"}, class: "current" %>
		<% else %>
			<%= link_to "Start", {controller: "pages", action: "index"} %>
		<% end %>
		<% for page in @other_pages do %>
			<% if page == @current_page %>
				<%= link_to page.display_title, page, class: "current" %>
			<% else %>
				<%= link_to page.display_title, page %>
			<% end %>
		<% end %>
	</div>
</div>
<div class="page">
	<h2>
		<%= @current_page.display_title %>
	</h2>
	<% if @current_page.photo %>
		<img src="http://lupa.at/img/<%= @current_page.photo.filename %>">
	<% end %>
	<p>
		<%= nl2br @current_page.display_text %>
	</p>
	
	<p>
		<%= link_to "Steindenkmäler suchen", {:controller => 'monuments', :action => 'search'}, class: 'button primary' %>
	</p>
	
	<h2>Neue Bilder seit <%= l @new_monuments_date, format: "%B %Y" %></h1>
	<div class="two-column">
		<%= render partial: 'monuments/monuments_list_with_more', locals: { monuments: @new_monuments } %>
	</div>
</div>