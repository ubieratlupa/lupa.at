<div class="page two-column">
	<h1>Neuzugänge <%= l @new_monuments_date, format: "%B %Y" %></h1>
	<%= paginate @new_monuments %>
	<%= render partial: 'monuments/monuments_list', locals: { monuments: @new_monuments, source: @source } %>
	<%= paginate @new_monuments %>
</div>