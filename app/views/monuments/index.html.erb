<div class="page">
	<h1><%= @index_page.title %></h1>
	<p>
		<%= nl2br @index_page.text %>
	</p>
	<div class="show-more show-all">
		<%= link_to "Alle Denkmäler anzeigen", @query %>
		<!-- <%= link_to "Denkmäler suchen", controller: "queries", action: "edit", id: @query %> -->
	</div>
	
	<%= render partial: 'queries/form', locals: { query: @query } %>
	<h2>Neuzugänge <%= l @new_monuments.first.created, format: "%B %Y" %></h2>
	<%= render partial: 'monuments/monuments_list', locals: { monuments: @new_monuments } %>
	<% if @new_monuments.total_count > @new_monuments.count %>
		<div class="show-more"><%= link_to "Alle #{@new_monuments.total_count} Neuzugänge anzeigen", action: "recent" %></div>
	<% end %>
</div>