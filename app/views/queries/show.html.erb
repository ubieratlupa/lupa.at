<div class="page">
	<%= render partial: 'queries/summary', locals: { query: @query, monuments: @monuments } %>
	<%= paginate @monuments %>
	<%= render partial: 'monuments/monuments_list', locals: { monuments: @monuments } %>
	<%= paginate @monuments %>
</div>