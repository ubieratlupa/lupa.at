<div class="place page">
	<ol class="breadcrumb">
		<% @place.hierarchy.each do | place | %>
			<li><%= link_to place.name, place %></li>
		<% end %>
	</ol>
	<h1><%= @place.name %></h1>
	<div class="section">
		<h3><%= @monuments_found.count %> Steindenkmäler gefunden in <%= @place.name %></h3>
		<%= render partial: 'monuments/monuments_list', locals: { monuments: @monuments_found } %>
	</div>
	<div class="section">
		<h3><%= @monuments_conserved.count %> Steindenkmäler verwahrt in <%= @place.name %></h3>
		<%= render partial: 'monuments/monuments_list', locals: { monuments: @monuments_conserved } %>
	</div>
</div>